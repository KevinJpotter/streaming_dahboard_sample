resources:
  dashboards:
    streaming_monitoring_dashboard:
      display_name: "Streaming Monitoring Dashboard — ${var.environment}"
      warehouse_id: ${var.warehouse_id}
      file_path: ../dashboard/lvdash.json
      dataset_catalog: ${var.catalog}
      dataset_schema: ${resources.schemas.monitoring.name}
      embed_credentials: false
